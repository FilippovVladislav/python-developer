---
description: "Модули Python: import, math, random, datetime, обращение module.name, from import и alias."
---

# Модули

::: {.callout-important}
Главный вопрос главы:

**Как использовать готовый код из других модулей Python?**
:::

После обработки ошибок программа становится устойчивее. Теперь можно расширять её возможности с помощью готового кода, который уже входит в Python.

## Что такое модуль

TODO: объяснить модуль как файл с готовыми функциями, значениями и инструментами.

## import и module.name

TODO: показать `import math` и обращение вида `math.sqrt()`.

## Несколько полезных модулей

TODO: кратко показать `math`, `random` и простой пример `datetime` без превращения главы в каталог стандартной библиотеки.

## from module import name

TODO: объяснить импорт конкретного имени и сравнить читаемость с обычным `import module`.

## import ... as ...

TODO: показать псевдоним модуля там, где он делает код короче и остаётся понятным.

## Откуда брать модули

TODO: объяснить стандартную библиотеку на концептуальном уровне и не углубляться в установку внешних пакетов.

## Что важно запомнить

Модуль позволяет использовать готовый код. Запись `module.name` часто помогает видеть, откуда пришла функция или значение.

## Что дальше?

Готовые модули полезны, но свои программы тоже можно разделять на файлы. Следующая глава покажет собственные модули.

### Практические задания

1. Используйте `math` для вычисления квадратного корня.

::: {.callout-note title="Ответ" collapse="true"}

TODO: ответ будет добавлен при полноценной разработке главы.

:::

2. Используйте `random` для выбора случайного элемента из списка.

::: {.callout-note title="Ответ" collapse="true"}

TODO: ответ будет добавлен при полноценной разработке главы.

:::

3. Импортируйте одну функцию через `from module import name`.

::: {.callout-note title="Ответ" collapse="true"}

TODO: ответ будет добавлен при полноценной разработке главы.

:::
