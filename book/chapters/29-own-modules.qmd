---
pagetitle: "Собственные модули Python: разделение программы на файлы"
description: "Собственные модули Python: разделение программы на main.py и helpers.py, импорт функций и базовый __main__."
keywords:
  - собственные модули Python
  - main.py
  - helpers.py
  - импорт функций
  - __main__ Python
  - организация кода
---

# Собственные модули

::: {.callout-important}
Главный вопрос главы:

**Как разделить свою программу на несколько Python-файлов?**
:::

Модули стандартной библиотеки показывают важную идею: код можно хранить в отдельных файлах и подключать там, где он нужен. Ту же идею можно применить к своим программам.

## Один .py как модуль

TODO: объяснить, что файл `helpers.py` может быть модулем для `main.py`.

## import helpers

TODO: показать импорт всего файла и вызов функции через `helpers.name()`.

## from helpers import ...

TODO: показать импорт конкретной функции и обсудить, когда явное происхождение имени полезнее.

## Основной файл программы

TODO: показать простую структуру `main.py` и `helpers.py`.

## Базовый __name__ и __main__

TODO: дать только начальное объяснение защитного блока, если глава будет использовать запуск модуля как программы.

## Зачем разделять код

TODO: связать разделение на модули с ростом менеджера задач и вынесением функций работы с задачами.

## Что важно запомнить

Собственный модуль — это обычный Python-файл, который можно импортировать. Разделение кода помогает держать основной сценарий программы понятным.

## Что дальше?

Когда связанных модулей становится несколько, их удобно объединить в пакет.

### Практические задания

1. Создайте идею структуры из двух файлов: `main.py` и `helpers.py`.

::: {.callout-note title="Ответ" collapse="true"}

TODO: ответ будет добавлен при полноценной разработке главы.

:::

2. Вынесите функцию приветствия в отдельный модуль.

::: {.callout-note title="Ответ" collapse="true"}

TODO: ответ будет добавлен при полноценной разработке главы.

:::

3. Опишите, какие функции менеджера задач можно вынести из `main.py`.

::: {.callout-note title="Ответ" collapse="true"}

TODO: ответ будет добавлен при полноценной разработке главы.

:::
